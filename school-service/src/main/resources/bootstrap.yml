server:
  port: 8031

spring:
  application:
    name: school-service
  cloud:
    config:
      uri: http://localhost:8888
#  profiles:
#    active: development

management:
  endpoints:
    web:
      exposure:
        include:
        - '*'