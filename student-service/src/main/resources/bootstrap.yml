server:
  port: 8030

  
  
spring:
  application:
    name: student-service
    
  cloud:
    config:
      uri: http://localhost:8888
#  profiles:
#    active: development

management:
  endpoints:
    web:
      exposure:
        include:
        - '*'    
   
